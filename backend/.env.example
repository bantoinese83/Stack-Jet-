# Environment variables for the backend

# This is used by the JWT authentication service
JWT_SECRET=a_very_secret_key_that_should_be_changed

# The following are provided by docker-compose for local development,
# but are listed here for clarity and for production environments.
# DATABASE_URL=postgresql://user:password@postgres:5432/nexusdb
# REDIS_URL=redis://redis:6379
